{
  "name": "functions",
  "description": "Firebase Cloud Functions for inventory alerts",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0",
    "@sendgrid/mail": "^8.0.0"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0 || true"
  }
}
