import React from "react";

// ProfilePage — simple placeholder; admin profile details are shown elsewhere
const ProfilePage = () => (
	<div style={{ padding: 24 }}>
		<h2>Profile</h2>
		<p>Your profile information will appear here.</p>
	</div>
);

export default ProfilePage;
