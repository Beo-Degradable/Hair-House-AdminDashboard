// Drawer placeholder About (legacy): consider removing after new About page adoption.
import React from "react";
const AboutPage = () => (
  <div style={{ padding: 32 }}>
    <h2>About (Legacy)</h2>
    <p>Legacy drawer version. Use main About page instead.</p>
  </div>
);
export default AboutPage;
